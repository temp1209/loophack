# CLAUDE.md — loophackプロジェクトガイド

このファイルはAIエージェント（Claude Code等）がこのリポジトリで作業する際に
最初に読む「プロジェクト憲法」です。作業を始める前に必ず目を通してください。

## 🚫 最重要: 個人情報・秘密情報を書かない

このリポジトリは**公開リポジトリ**です。以下は理由を問わず一切コミットしないでください。
「わかりやすい説明のための例」であっても例外はありません。

- 実メールアドレス、実氏名、電話番号、住所などの個人を特定できる情報
- APIキー、トークン、パスワード等の秘密情報
- 上記を含む可能性のあるログ・スクリーンショット・生レスポンスのダンプ

コミット前に `./scripts/check-no-pii.sh` を実行し、CIでも同じチェックが
push/PRごとに走ります（詳細は本ファイル末尾）。ダミーが必要な場合は
`user@example.com` のような明示的なプレースホルダーのみ使用してください。

## プロジェクト概要

`loophack` は、日常の行動をなるべく手入力なしに自動記録し、可視化することで
「良い行動の継続」につなげることを目指すAndroidアプリです。目的は「無意識化」と
「可視化」のどちらが効くかを検証することではなく、**記録の仕組みを作ること自体を
楽しみながら、生活が実際に良くなった実感を得ること**にあります。この前提を離れて
機能を足さないこと（要求定義の元は `docs/requirements.md` を参照）。

- **Phase 1（現在着手中）**: スマホ単体のセンサー/API（Wi-Fi接続変化、Bluetooth接続変化、
  Activity Recognition API、充電状態、Sleep API等）で検知できる行動（睡眠、帰宅/外出、
  仮眠など）を対象とする
- **Phase 2以降**: NFCタグ（購入済み・浴室/洗濯機に設置予定）やHome Assistant連携が
  必要な行動（入浴、洗濯、歯磨き、プランク等）。Home Assistant自体は別マシン・別
  リポジトリで管理し、このリポジトリはAndroidアプリ本体に集中する
- 主観指標（気分・疲労度など）も、時間トリガーで問いかける形で記録対象に含む

技術スタックは未確定（`docs/requirements.md` の「未決事項」を参照）。決定次第、
このセクションと次の「よく使うコマンド」を更新すること。

## よく使うコマンド

*(技術スタック未確定のため未記載。決定後にビルド/テスト/lintの実行コマンドを
ここに追記すること。空のままエージェントに推測させない。)*

## アーキテクチャ

*(実装開始前のため詳細未定。以下は要求定義段階で合意済みの設計方針で、
実装時もこれを崩さないこと)*

- 記録データは「行動種別ごとの専用テーブル」ではなく、共通の **Event抽象**
  （行動種別／開始時刻／終了時刻（nullなら瞬間イベント）／検知トリガー種別／
  確度・信頼度／手動補正フラグ／メタデータ）を核とする。Phase 2で行動種別や
  トリガー種別が増えてもスキーマ自体は変えず拡張できるようにする
- 検知トリガーの解釈が曖昧な場合（例: Wi-Fi切断＝外出か電波不調か）は、
  自動記録＋信頼度スコア＋あとからの手動補正で解決する。無理に自動判定の
  精度だけで完結させようとしない
- 主観指標（気分・疲労度）は別種のイベントとして同じEvent抽象に乗せ、
  行動記録と時系列で突き合わせられるようにする

## 開発時の注意点

各ルールは背景（過去の事故・失敗、または設計意図）とセットで書く。理由を
消して箇条書きだけ残さないこと。

- **秘密情報を直書きしない**: 公開リポジトリのため、APIキー等は
  EncryptedSharedPreferences等の暗号化ストレージにのみ保持し、バックアップ機能の
  エクスポート対象からも明示的に除外する（kakeibo-app開発での運用ルールを踏襲）
- **個人情報をコミットしない**: 上記「最重要」セクション参照。検出スクリプトと
  CIの二重チェックで機械的に担保する（意思だけに頼らない）
- **DBスキーマ変更時は必ず手書きマイグレーションを追加する**: 自動マイグレーション
  任せにすると、Event抽象のような柔軟なスキーマでは意図しないデータ欠損が
  起きやすい。バージョン番号とマイグレーションリストを同時に更新すること
- **センサー/API由来の生イベントデータは保存する**: 再現性確保のため、パース前の
  生レスポンス（Wi-Fi状態変化のペイロード等）を保存し、パース処理はそれを
  再読込すれば再現できる設計にする。誤検知のデバッグや信頼度スコアの
  チューニングに直接必要になる
- **例外を握りつぶさない**: ユーザー向けメッセージと内部の状態遷移
  （PENDING/RUNNING/DONE/FAILED等）を明確に分離して扱う。バックグラウンド
  センサー処理は失敗が目立ちにくいため特に注意する
- **行動種別をハードコードで閉じない**: Phase 1の実装であっても、行動種別を
  将来追加できる前提（マスタ管理 or enum拡張しやすい設計）で作る。企画の
  スコープ拡張（Phase 2）を見越した要求である

## ドキュメントの入口

要件定義・実装計画・バックログ・引き継ぎメモなどは `docs/README.md` を
起点に参照すること。
