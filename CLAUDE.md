# CLAUDE.md — loophackプロジェクトガイド

このファイルはAIエージェント（Claude Code等）がこのリポジトリで作業する際に
最初に読む「プロジェクト憲法」です。作業を始める前に必ず目を通してください。

## 🚫 最重要: 個人情報・秘密情報を書かない

このリポジトリは**公開リポジトリ**です。以下は理由を問わず一切コミットしないでください。
「わかりやすい説明のための例」であっても例外はありません。

- 実メールアドレス、実氏名、電話番号、住所などの個人を特定できる情報
- APIキー、トークン、パスワード等の秘密情報
- 上記を含む可能性のあるログ・スクリーンショット・生レスポンスのダンプ

コミット前に `./scripts/check-no-pii.sh` を実行し、CIでも同じチェックが
push/PRごとに走ります（詳細は本ファイル末尾）。ダミーが必要な場合は
`user@example.com` のような明示的なプレースホルダーのみ使用してください。

## プロジェクト概要

`loophack` は、日常の行動をなるべく手入力なしに自動記録し、可視化することで
「良い行動の継続」につなげることを目指すAndroidアプリです。目的や対象範囲、
合意済みの設計方針、未決事項は `docs/requirements.md` に集約しているので、
作業前に必ず読むこと。この前提を離れて機能を足さないこと。

## よく使うコマンド

*(技術スタック未確定のため未記載。決定後にビルド/テスト/lintの実行コマンドを
ここに追記すること。空のままエージェントに推測させない。)*

## 開発時の注意点

各ルールは背景（過去の事故・失敗、または設計意図）とセットで書く。理由を
消して箇条書きだけ残さないこと。

- **秘密情報を直書きしない**: 公開リポジトリのため、APIキー等は
  EncryptedSharedPreferences等の暗号化ストレージにのみ保持し、バックアップ機能の
  エクスポート対象からも明示的に除外する（kakeibo-app開発での運用ルールを踏襲）
- **個人情報をコミットしない**: 上記「最重要」セクション参照。検出スクリプトと
  CIの二重チェックで機械的に担保する（意思だけに頼らない）
- **DBスキーマ変更時は必ず手書きマイグレーションを追加する**: 自動マイグレーション
  任せにすると、Event抽象のような柔軟なスキーマでは意図しないデータ欠損が
  起きやすい。バージョン番号とマイグレーションリストを同時に更新すること
- **センサー/API由来の生イベントデータは保存する**: 再現性確保のため、パース前の
  生レスポンス（Wi-Fi状態変化のペイロード等）を保存し、パース処理はそれを
  再読込すれば再現できる設計にする。誤検知のデバッグや信頼度スコアの
  チューニングに直接必要になる
- **例外を握りつぶさない**: ユーザー向けメッセージと内部の状態遷移
  （PENDING/RUNNING/DONE/FAILED等）を明確に分離して扱う。バックグラウンド
  センサー処理は失敗が目立ちにくいため特に注意する
- **行動種別をハードコードで閉じない**: 初期実装であっても、行動種別を将来
  追加できる前提（マスタ管理 or enum拡張しやすい設計）で作る。企画のスコープ
  拡張を見越した要求である

## ドキュメント運用ルール

- **要件定義書・実装計画書はそれぞれ1つに絞る**: `docs/requirements.md` と
  `docs/implementation-plan.md` を分散させない。ドキュメントが散らばると
  参照漏れが起きやすく、AIエージェントのコンテキストも無駄に消費するため
- **要件変更は上書きせず変更履歴に追記する**: `docs/requirements.md` 末尾の
  変更履歴に、変更理由とともに追記すること
- **実装計画書はTODOリストとして運用する**: `docs/implementation-plan.md` は
  フェーズで区切らず、機能・修正単位でTODO項目を積む。完了項目は削除せず✅を付ける
- **フェーズ制は使わない**: 「Phase 1/Phase 2」のような区切りは境界が曖昧に
  なりやすいため使わない。機能・修正単位で区切る
- **要件定義を振り返るタイミングを決めておく**: 1つの機能を実装し終えた
  タイミングで `docs/requirements.md` を見直し、ズレがあれば変更履歴に追記する
- **ドキュメント整理はマージ前に挟む**: 実装中に増えた記述の整理・重複排除は
  マージ前にまとめて行う

## ブランチ運用ルール

- **1ブランチ = 1機能・修正単位**: フェーズ制を使わないため、ブランチも
  機能・修正の単位で切る。まとまったら都度mainにマージする（まとめて
  大きくマージしない）
- **バグを見つけても元のブランチには戻らない**: 例えば機能Aをマージ後、
  機能Bの作業中にAのバグを見つけた場合、Aのブランチ（マージ済み）を掘り
  起こすのではなく、mainから新しく `fix/...` ブランチを切って修正し、
  mainにマージする。マージ済みブランチの再利用は他の変更が混ざるなど
  事故りやすいため
- **他の作業ブランチへの反映は `git pull origin main` で取り込む**:
  作業中のブランチ（上記の例のB）で最新のmainを取り込みたい場合は、
  そのブランチ上で `git pull origin main` を実行する（fetch+mergeが
  1コマンドで行われる）

## ドキュメントの入口

要件定義・実装計画・引き継ぎメモなどは `docs/README.md` を起点に参照すること。
